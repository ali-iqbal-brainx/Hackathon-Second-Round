# Backend API root — must match Nest global prefix + host/port
# Example when API runs locally on port 4000:
VITE_API_URL=http://localhost:4000/api/v1

# Optional fallback if VITE_API_URL is not set (same shape as above)
# VITE_API_BASE_URL=http://localhost:4000/api/v1
